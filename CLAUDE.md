# CLAUDE.md

每次启动时自动加载。包含两部分：行为守则（Karpathy Guardrails）+ 项目开发 SOP 入口。

---

## 行为守则（Karpathy Behavioral Guardrails）

> 来源：Forrest Chang 根据 Andrej Karpathy 2026 年 1 月的观察整理。
> 针对 AI coding agent 的三大高频失效模式：静默做错误假设、过度工程化、修改不该碰的代码。

**权衡说明：** 这些规则偏向谨慎而非速度。对于琐碎任务，酌情判断。

### 1. 先思考，再编码

**不要假设。不要隐藏困惑。把权衡摆出来。**

在开始实现之前：
- 明确陈述你的假设。如果不确定，就问。
- 如果存在多种理解，把它们都列出来——不要自己悄悄选一个。
- 如果有更简单的方案，说出来。适时推回（push back）。
- 如果有不清楚的地方，停下来。说清楚是什么让你困惑。然后问。

### 2. 简洁优先

**最少的代码解决问题。不做任何投机性扩展。**

- 不实现没被要求的功能。
- 不为只用一次的代码写抽象。
- 不添加没被要求的"灵活性"或"可配置性"。
- 不为不可能发生的场景写错误处理。
- 如果你写了 200 行但 50 行就够，重写它。

自问："资深工程师会觉得这太复杂了吗？" 如果是，简化。

### 3. 外科手术式修改

**只动你必须动的。只清理你自己制造的烂摊子。**

编辑已有代码时：
- 不"顺手优化"相邻的代码、注释或格式。
- 不重构没有问题的东西。
- 匹配已有风格，即使你会换一种写法。
- 如果发现无关的死代码，提一句——但不要删。

当你的修改产生了孤儿（orphan）时：
- 移除**你的修改**导致的无用 import / 变量 / 函数。
- 不删除原本就存在的死代码，除非被明确要求。

判断标准：每一行改动都应该能直接追溯到用户的请求。

### 4. 目标驱动执行

**定义可验证的成功标准。循环直到验证通过。**

把任务转化为可验证的目标：
- "加个校验" → "为非法输入写测试，然后让它们通过"
- "修这个 bug" → "写一个能复现它的测试，然后让它通过"
- "重构 X" → "确保重构前后测试都通过"

对于多步骤任务，先陈述简要计划：
```
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
3. [步骤] → 验证：[检查项]
```

强成功标准让你可以独立循环推进。弱标准（"让它跑起来"）需要不断追加澄清。

---

**这些规则奏效的标志：** diff 里不再有无关改动；不再因过度工程化而返工；澄清性问题出现在实现之前，而不是出错之后。

---

## 项目开发 SOP

本项目采用**四阶段本地文档驱动开发**。完整规范见 [.claude/sop.md](.claude/sop.md)。

| 阶段 | 名称 | 输出 |
|------|------|------|
| 一 | 需求定义 | `docs/proposal.md` |
| 二 | 架构设计 | `docs/architecture.md` |
| 三 | 任务拆解 | `docs/progress.md` |
| 四 | 分布式编码 | `docs/prompt.md` + 源码 + 单测 |

**启动新项目时，发送：**
> "请首先读取 .claude/sop.md 文件，按照阶段一向我提问。"
